# The Shared Frame as a substrate -- what plugs in

> The whole workspace is converging into one frontier flagship. Each project shape is both an
> **individual use case** and a **cell in a growing substrate**. This page is the first public surface
> where that substrate is *visible and re-checkable*: one spine -- **perceive · judge · prove · refine ·
> actuate** -- that every shape attaches to. This map says, honestly, what is wired into the demo today
> and where the next shapes socket in.

## The spine (one operation, the reconcile)

```
            YOU ─────────────┐                    ┌───────────── THE MODEL
      (actuate, perceive)    │                    │     (actuate, perceive)
                             ▼                    ▼
   plug-in media ──▶ [ PERCEIVE ] ──▶ [ JUDGE ] ──▶ [ PROVE ] ──▶ certificate you re-derive
   generated frame    (the eye)      (criteria)    (the bar)            │
                             ▲                                          │
                             └──────────── [ REFINE / TRANSFORM ] ◀─────┘
                                    (both witnesses change the substrate)
```

## Wired into this demo today

| Shape | Individual use case | Role in this substrate | In the demo |
|---|---|---|---|
| **coherence-membrane** | the accountability spine -- `Certificate`, the eye (`VisualArtifactOrgan`), perceptual hash, reconcile | defines *perceive · judge · prove* | `verdict.js` (the bar) + `eye.js` (the eye), faithful ports gated to Python |
| **studio-engine** | generative + verification engine (generate→judge→refine→certify) | the *generate* half + the criteria | `engine.js`, gated bit-for-bit to the Python `final_score` |
| **the field rasterizer** (cm) | make field generators perceivable | lets the eye see gyroid/quasicrystal as real fields | the baked field fixtures the page renders |
| **render lineage** (raw → reference-chamber) | client-side WebGL/Canvas rendering of verified math | the human aperture | `render.js`, lifted verbatim |
| **cdev-ledger** | accountable c:/dev ground-truth | record-on-ship / recall-verify | the artifact is recorded (`showcase-shared-frame`) |
| **transpile-conservation principle** | faithfulness is criterion-relative + externally witnessed | the *gate*: JS readout reconciles to the Python criterion | `engine.test` + `eye.test` ARE that reconcile |

## The next sockets (named, not yet wired)

| Shape | Individual use case | How it plugs into this substrate |
|---|---|---|
| **raw** (native two-way rendering engine) | GPU-native, real-time rendering + simulation | upgrade the human aperture from WebGL fragments to the full native engine; the local cockpit behind the same shared frame |
| **accountable-surface** (3-channel fs/web/os actuation + bilateral eye) | bounded, witnessed actuation; live screen capture | promote actuation from canvas transforms to *real* gated actions; the bilateral-eye live-screen capture becomes the "watch together" feed |
| **statechain / livestate** | tamper-evident replayable diff-chain of a perceived sequence | the witnessed-sequence backend for video "watch together" -- every frame hashed into a re-checkable chain |
| **quanta (qdb / qsql / qkv)** | native zero-dep data/query suite | the relational/static plane of the substrate -- witnessed data alongside witnessed pixels |
| **context_pack / spatial-context-engine** | relational map of a codebase (topology + roles) | perceive *the flagship itself* -- the substrate map as a navigable, witnessed artifact |
| **workspace-repo-map / factory** | productize + dependency-map sprawling repos; resist agent-induced architecture drift | the sensor + assembler that turns "all project shapes" into one coherent flagship |
| **the center module** (cm) | the neutral center: two minds reconcile a subject under a named criterion | the orchestration behind this page -- atelier-mind + eye-mind meeting at one witnessed verdict |
| **provenance / origin_criterion** | compose an artifact's origin verdict | witness where a plugged-in photo/video *came from*, not just what it looks like |
| **perceptv** | live terminal channel of the witnessed loop | the same witnessed stream, rendered for a different surface |
| **portfolio-site** | the outward face | hosts this page; the public front door to the flagship |

## The honest line

Today the demo wires **perceive · judge · prove · refine/transform** over two substrates (your own
media, and a generated frame), with every faithful claim gated against the Python spine. The next
sockets -- native rendering (raw), real actuation (accountable-surface), witnessed sequences
(statechain), the data plane (quanta), and self-perception (context_pack) -- are real shapes with
shipped cores, named here so the growth path is a *map*, not a promise. Each is a tool in its own
right; each is also a cell. The flagship is the substrate they all reconcile into.
